<%= link_to "作成", new_blog_path%>
<% @blog.each do |blog| %>

  <%= blog.title %>
  <%= blog.content %>
  <%= link_to 'show', blog_path(blog.id) %>
  <%= link_to 'edit', edit_blog_path(blog.id) %>
  <%= link_to 'delete', blog_path(blog.id) , method: :delete %>

<% end %>